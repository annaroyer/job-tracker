All Jobs for <strong><%= @company.name %></strong>

<% @jobs.each do |job| %>
  <ul><%= link_to job.title, company_job_path(@company, job) %>
    <li>
      <%= link_to 'Edit', edit_company_job_path(@company, job) %>
    </li>
    <li>
      <%= link_to 'Delete', company_job_path(@company, job), method: :delete, data: {confirm: "Really delete the job?"} %>
    </li>
  </ul>
<% end %>
