<%= form_for @company do |f| %>
<ul>
  <% @company.errors.full_messages.each do |error| %>
      <li><%= error %></li>
  <% end %>
</ul>
<p>
  <%= f.label :name %>
  <%= f.text_field :name %>
</p>
<p>
  <%= f.submit %>
</p>
<% end %>
