<%= form_for [@job, @comment] do |f| %>
  <ul>
    <% @comment.errors.full_messages.each do |error| %>
      <li><%= error %></li>
    <% end %>
  </ul>
  <h4><%= f.text_area :content, class: "form-control" %></h4>
  <p><%= f.submit class: "btn  btn-block" %></p>
<% end %>
